# Known-safe values that are PUBLIC by design — they ship in the site's client-side
# HTML/JS for the browser to use, so they are NOT secrets/leaks. The pre-commit hook
# skips any added line containing one of these. One substring per line; # starts a
# comment. Only add values you've confirmed grant no private access.

# Cloudflare Web Analytics beacon token (write-only pageview telemetry for mnm-db.com;
# grants no account/data access — it's meant to live in the page HTML):
dccb9da3e29b4fd5a63731c54450dd1c

# Cloudflare Turnstile site key (public widget key; the secret half lives on the Worker):
0x4AAAAAADsBgp1uszsr9gUW
